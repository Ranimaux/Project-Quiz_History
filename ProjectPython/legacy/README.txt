Repository "legacy" is the old version of "projectQuiz".
This version is not supported for new modification and have not interface graphics, only in shell.

Python 3.0 are requis to launch this version, if you use this.